import React from "react";
import { Text, View } from "react-native";

const Maps = () => {
  return (
    <View
      style={{
        padding: 10,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        marginBottom: 20,
        height: "50%",
        backgroundColor: "pink",
      }}
    >
      <Text style={{ color: "white", fontSize: 25 }}>Maps</Text>
    </View>
  );
};

export default Maps;
